function renderPcSourceSetup() {
  return `<section class="grid grid-2">
    <div class="card-panel"><div class="card-title-row"><h3>OBS / encoder connection</h3><span class="badge ${S.status?.sources?.pc?.live ? "green" : ""}">${S.status?.sources?.pc?.live ? "CONNECTED" : "WAITING"}</span></div><p>Use these values in OBS → Settings → Stream → Custom.</p><label>Server</label><div class="copy-field"><input readonly value="${esc(S.status?.pcServer || "")}"><button class="btn btn-ghost btn-sm" data-copy="${esc(S.status?.pcServer || "")}">Copy</button></div><label>Stream key</label><div class="copy-field"><input readonly value="${esc(S.status?.pcKey || "")}"><button class="btn btn-ghost btn-sm" data-copy="${esc(S.status?.pcKey || "")}">Copy</button></div></div>
    <div class="card-panel"><h3>Key security</h3><p>This PC key is generated by CastNexus and is separate from your real Twitch stream key. Rotate it if an OBS config or screenshot leaks.</p><button class="btn btn-danger btn-sm" data-action="regen-pc-key">Regenerate PC ingest key</button></div>
  </section>`;
}

function renderConsoleSourceSetup() {
  return `<section class="grid grid-2">
    <div class="card-panel"><div class="card-title-row"><h3>Console capture</h3><span class="badge ${S.status?.sources?.console?.live ? "green" : ""}">${S.status?.sources?.console?.live ? "CONNECTED" : "WAITING"}</span></div><p>Point the console at the CastNexus host for DNS capture. The intercept container is required for reliable Twitch ingest capture.</p><label>CastNexus / DNS host</label><div class="copy-field"><input readonly value="${esc(S.status?.mediaHost || "")}"><button class="btn btn-ghost btn-sm" data-copy="${esc(S.status?.mediaHost || "")}">Copy</button></div><label>Twitch key</label><div class="code-note">${esc(S.status?.streamKeyMasked || "Not configured")}</div></div>
    <div class="card-panel"><h3>Change console Twitch key</h3><p>The saved key is masked and never returned in full. Replace it here if Twitch rotates your stream key.</p><button class="btn btn-primary btn-sm" data-action="change-stream-key">Replace Twitch stream key</button></div>
  </section>`;
}

function renderMusicSourceSetup() {
  const p=activeProfile();
  const running = S.status?.live && p?.mode === "music";
  return `<section class="grid grid-2">
    <div class="card-panel"><div class="card-title-row"><h3>Docker music broadcaster</h3><span class="badge ${running ? "green" : "cyan"}">${running ? "ON AIR" : S.tracks.length ? "STARTING / WAITING" : "NEEDS MUSIC"}</span></div><p>The <code>music24</code> worker watches this active profile, renders its ${p?.canvasMode === "vertical" ? "9:16" : "16:9"} spectrum scene and publishes only this profile's isolated music library.</p><div class="callout">Switch to another Music profile and CastNexus switches to that profile's separate tracks and file path.</div></div>
    <div class="card-panel"><h3>Music readiness</h3><p>${S.tracks.length ? `${S.tracks.length} track${S.tracks.length === 1 ? "" : "s"} in ${esc(p?.name || "this profile")}. Loop is ${S.musicSettings.loop ? "enabled" : "disabled"}.` : "Upload at least one audio file into this profile before the 24/7 broadcaster can start."}</p><button class="btn btn-primary btn-sm" data-nav="music">Open profile music</button></div>
  </section>`;
}

function renderDestinations() {
  const dests = S.status?.destinations || [];
  return `
    ${pageHead("MULTI-DESTINATION FAN-OUT", "Destinations", "Publish one source to normal 16:9 outputs, vertical 9:16 outputs, or untouched passthrough routes.", `<button class="btn btn-primary" data-action="add-destination">＋ Add destination</button>`)}
    <section class="card-panel">
      <div class="card-title-row"><h3>Output routes</h3><span class="badge">${dests.length} configured</span></div>
      <p>Each destination chooses its own layout. This lets one profile send YouTube's horizontal key and vertical/Shorts key at the same time.</p>
      <div class="callout" style="margin-bottom:12px">Source/passthrough uses stream copy. Forced 16:9 or 9:16 uses FFmpeg video transcoding, so those routes consume more CPU/GPU.</div>
      <div class="list-stack">${dests.length ? dests.map(destinationRow).join("") : `<div class="empty-state"><strong>No destinations yet</strong>Add an RTMP, RTMPS or SRT destination and choose Source, 16:9 or 9:16.</div>`}</div>
    </section>`;
}

function destinationRow(d) {
  const layout=d.layout||"source";
  const layoutLabel=layout==="vertical"?"9:16 VERTICAL":layout==="landscape"?"16:9 LANDSCAPE":"SOURCE";
  const platform=window.CastNexusDestinationPresets?.byId(d.platform)||(window.CastNexusDestinationPresets?.presets||[]).find(p=>String(d.name||"").toLowerCase().startsWith(p.name.toLowerCase()))||{mark:"↗",colour:"#8d73ff"};
  return `<div class="list-row destination-item"><div class="destination-icon" style="color:${esc(platform.colour)}">${esc(platform.mark)}</div><div class="item-main"><strong>${esc(d.name)}</strong><span>${esc(d.urlMasked || "")} · ${esc(layoutLabel)}</span></div><span class="badge ${layout === "vertical" ? "cyan" : layout === "landscape" ? "purple" : ""}">${layoutLabel}</span><span class="badge ${d.active ? "green" : d.enabled ? "purple" : ""}">${d.active ? "PUBLISHING" : d.enabled ? "ENABLED" : "OFF"}</span><div class="item-actions"><label class="toggle"><input type="checkbox" data-dest-toggle="${esc(d.id)}" ${d.enabled ? "checked" : ""}><span class="toggle-track"></span></label><button class="icon-button" data-edit-dest="${esc(d.id)}" title="Edit">✎</button><button class="icon-button" data-delete-dest="${esc(d.id)}" title="Delete">×</button></div></div>`;
}

function builtinCard(key, label, path, description) {
  const cfg = S.overlayConfig[key] || {};
  return `<div class="card-panel"><div class="card-title-row"><div><h3>${esc(label)}</h3><p>${esc(description)}</p></div><span class="badge purple">BUILT-IN</span></div><div class="copy-field"><input readonly value="${esc(overlayUrl(path))}"><button class="btn btn-ghost btn-sm" data-copy="${esc(overlayUrl(path))}">Copy</button></div><div class="page-actions" style="margin-top:10px"><button class="btn btn-ghost btn-sm" data-edit-builtin="${key}">Edit</button><button class="btn btn-ghost btn-sm" data-open-url="${esc(overlayUrl(path))}">Preview</button></div><div class="stat-sub">${esc(cfg.title || label)} · ${esc(cfg.accent || "default accent")}</div></div>`;
}

function overlayKind(o) {
  if (o.type === "html" && (o.config?.kind === "iframe" || o.config?.kind === "browser")) return "Browser / iframe";
  if (o.type === "html") return "HTML / CSS";
  if (o.type === "text") return "Text";
  if (o.type === "music") return "Music scene";
  return o.type;
}

function renderStudio() {
  const overlays = visibleOverlays();
  return `
    ${pageHead("SCENES & BROWSER SOURCES", "Overlay Studio", "Build broadcast scenes, add StreamElements/Streamlabs browser widgets, raw HTML/CSS, text and profile-scoped music visuals.", `<button class="btn btn-primary" data-action="add-overlay">＋ New overlay</button>`)}
    <section class="grid grid-2">
      <div class="card-panel"><div class="card-title-row"><h3>Master scene switcher</h3><span class="badge cyan">ONE OBS URL</span></div><p>Add this browser source once. Scene buttons below swap content live over SSE without reconfiguring OBS.</p><div class="copy-field"><input readonly value="${esc(overlayUrl("master"))}"><button class="btn btn-ghost btn-sm" data-copy="${esc(overlayUrl("master"))}">Copy</button></div><div class="section-title">Program scene</div>${sceneButtons()}</div>
      <div class="card-panel"><div class="card-title-row"><h3>Sandboxed browser overlays</h3><span class="badge green">STREAM ELEMENTS READY</span></div><p>Website overlays are rendered inside a sandboxed iframe with scripts, same-origin and presentation support. That is enough for common alert widgets while avoiding popup/forms/top-navigation permissions.</p><div class="callout">Use Browser / iframe for StreamElements, Streamlabs, alert boxes, chat widgets or any trusted browser-source URL. Use HTML / CSS for code you control.</div></div>
    </section>
    <div class="section-title">Built-in scenes</div>
    <section class="grid grid-3">
      ${builtinCard("startingSoon", "Starting Soon", "starting-soon", "Animated standby scene with optional countdown and optional active-profile music.")}
      ${builtinCard("brb", "BRB", "brb", "Intermission scene that can play only the active profile's music library.")}
      ${builtinCard("ending", "Ending", "ending", "Off-air scene with social handles and optional active-profile music.")}
    </section>
    <div class="section-title">Custom overlay library</div>
    <section class="card-panel"><div class="list-stack">${overlays.length ? overlays.map(overlayRow).join("") : `<div class="empty-state"><strong>No custom overlays</strong>Create a browser-source, HTML/CSS, text or music overlay.</div>`}</div></section>`;
}

function overlayRow(o) {
  const kind = overlayKind(o);
  const url = overlayUrl(`custom/${o.slug}`);
  const canActivate = o.type === "text" || o.type === "html";
  const icon = o.type === "music" ? "♫" : kind.startsWith("Browser") ? "⊙" : o.type === "text" ? "T" : "&lt;/&gt;";
  return `<div class="list-row overlay-item"><div class="overlay-icon">${icon}</div><div class="item-main"><strong>${esc(o.name)}</strong><span>${esc(kind)} · /${esc(o.slug)}</span></div><span class="badge ${kind.startsWith("Browser") ? "green" : o.type === "music" ? "cyan" : "purple"}">${esc(kind.toUpperCase())}</span><div class="item-actions">${canActivate ? `<button class="btn btn-ghost btn-sm" data-activate-overlay="${esc(o.id)}">Show</button>` : ""}<button class="icon-button" data-copy="${esc(url)}" title="Copy URL">⧉</button><button class="icon-button" data-open-url="${esc(url)}" title="Open">↗</button><button class="icon-button" data-edit-overlay="${esc(o.id)}" title="Edit">✎</button><button class="icon-button" data-delete-overlay="${esc(o.id)}" title="Delete">×</button></div></div>`;
}

// Kept as a fallback renderer. dashboard/public/app-music-page.js overrides
// this with the dedicated profile-aware control room after scripts load.
function renderMusic() {
  const p = activeProfile();
  const visual = p?.musicVisual || {};
  const musicProfile = p?.mode === "music";
  return `
    ${pageHead("PROFILE AUDIO", musicProfile ? "Music 24/7" : "Profile Music", `Library isolated to ${p?.name || "the active profile"}.`, `<label class="btn btn-primary" style="margin:0"><input id="music-file-input" type="file" accept="audio/*" multiple hidden>＋ Upload music</label>`)}
    <section class="card-panel"><div class="card-title-row"><h3>Profile music program</h3><span class="badge cyan">${p?.canvasMode === "vertical" ? "9:16" : "16:9"}</span></div><div class="preview-frame"><iframe src="${esc(profileMusicUrl(p))}" allow="autoplay; fullscreen"></iframe></div></section>
    <section class="card-panel"><div class="form-grid"><div><label>Accent</label><input id="music-visual-accent" type="color" value="${esc(visual.accent || "#00f0ff")}"></div><div><label>Station label</label><input id="music-visual-station" value="${esc(visual.station || "CastNexus Radio")}"></div><div class="full"><label>Background image URL</label><input id="music-visual-background" placeholder="https://…" value="${esc(visual.background || "")}"></div><div><label>Fallback title</label><input id="music-visual-title" value="${esc(visual.title || "CastNexus Radio")}"></div><div><label>Cover / logo URL</label><input id="music-visual-cover" value="${esc(visual.cover || S.status?.profileImageUrl || "")}"></div></div></section>`;
}

function fmtDuration(sec) {
  sec = Math.max(0, Number(sec) || 0);
  const m = Math.floor(sec/60), s = Math.floor(sec%60);
  return `${m}:${String(s).padStart(2,"0")}`;
}

function trackRow(t) {
  return `<div class="list-row track-item"><div class="track-icon">♫</div><div class="item-main"><strong>${esc(t.title || "Untitled")}</strong><span>${esc(t.artist || "Unknown artist")} · ${fmtDuration(t.durationS)}</span></div><span class="badge">${Math.max(0, Math.round((t.sizeBytes || 0)/1024/1024*10)/10)} MB</span><div class="item-actions"><button class="icon-button" data-edit-track="${esc(t.id)}">✎</button><button class="icon-button" data-delete-track="${esc(t.id)}">×</button></div></div>`;
}

function renderProfiles() {
  return `
    ${pageHead("WORKSPACES", "Profiles", "Each profile has its own music library, canvas, destinations, scene and compositor state.", `<button class="btn btn-ghost" data-action="save-profile">Save active state</button>`)}
    <section class="grid grid-3">
      <button class="mode-card" data-add-profile-mode="pc"><span class="mode-icon">◫</span><strong>New PC profile</strong><small>OBS / encoder input with its own scenes and BRB music.</small><span class="mode-chip">CREATE PROFILE</span></button>
      <button class="mode-card" data-add-profile-mode="console"><span class="mode-icon">⌁</span><strong>New Console profile</strong><small>Console capture with independent outputs, scene music and canvas.</small><span class="mode-chip">CREATE PROFILE</span></button>
      <button class="mode-card music-card" data-add-profile-mode="music"><span class="mode-icon">♫</span><strong>New Music profile</strong><small>Always-on Docker radio with its own files, spectrum and layout.</small><span class="mode-chip">CREATE PROFILE</span></button>
    </section>
    <div class="section-title">Saved profiles</div>
    <section class="grid grid-2">${S.profiles.map(profileCard).join("")}</section>`;
}

function profileCard(p) {
  const active = p.id === S.activeProfileId;
  return `<div class="card-panel profile-card ${active ? "active-profile" : ""}"><div class="profile-icon" style="color:${esc(p.color || PROFILE_COLORS[p.mode])}">${p.mode === "music" ? "♫" : p.mode === "console" ? "⌁" : "◫"}</div><div class="item-main"><strong>${esc(p.name)}</strong><span>${esc(MODE_LABELS[p.mode] || p.mode)} · ${p.canvasMode === "vertical" ? "9:16 Vertical" : "16:9 Landscape"}</span><div class="profile-meta">${currentProfileBadge(p)}<span class="badge">${(p.destinationEnabledIds || []).length} outputs</span><span class="badge">ISOLATED MUSIC</span><span class="badge">${p.mode === "music" ? "AUTO 24/7" : p.sceneMusicEnabled ? "SCENE MUSIC" : p.compositorEnabled ? "COMPOSITOR" : "BROWSER OVERLAYS"}</span></div></div><div class="item-actions">${!active ? `<button class="btn btn-primary btn-sm" data-activate-profile="${esc(p.id)}">Switch</button>` : `<span class="badge green">ACTIVE</span>`}<button class="icon-button" data-edit-profile="${esc(p.id)}">✎</button>${S.profiles.length > 1 ? `<button class="icon-button" data-delete-profile="${esc(p.id)}">×</button>` : ""}</div></div>`;
}
