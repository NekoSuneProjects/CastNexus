name: Publish Docker Image

on:
  push:
    branches: [intercept]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker-publish:
    runs-on: [self-hosted, Linux, docker, buildx]
    steps:
      - uses: actions/checkout@v4
      - name: Read version from main
        id: version
        run: echo "value=$(curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/VERSION | tr -d '[:space:]')" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push intercept image
        uses: docker/build-push-action@v6
        with:
          context: ./intercept
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            CASTNEXUS_REVISION=${{ github.sha }}
          tags: |
            ghcr.io/nekosuneprojects/castnexus-intercept:latest
            ghcr.io/nekosuneprojects/castnexus-intercept:${{ steps.version.outputs.value }}
          labels: |
            org.opencontainers.image.title=CastNexus intercept
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=castnexus-intercept
          cache-to: type=gha,mode=max,scope=castnexus-intercept
