{
  "name": "castnexus-desktopapp-repo",
  "version": "0.1.0",
  "description": "CastNexus desktop app - Electron wrapper for the CastNexus dashboard",
  "license": "MIT",
  "repository": "NekoSuneProjects/CastNexus",
  "private": true,
  "scripts": {
    "fetch:dashboard": "bash fetch-dashboard.sh",
    "install:all": "npm run fetch:dashboard && npm --prefix electron install",
    "dev": "npm --prefix electron run dev",
    "start": "npm --prefix electron start",
    "build": "npm --prefix electron run build"
  },
  "workspaces": [
    "electron"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
