{
  "name": "castnexus-cli-repo",
  "version": "1.3.2",
  "description": "CastNexus CLI - headless install/launch for the CastNexus dashboard",
  "license": "MIT",
  "repository": "NekoSuneProjects/CastNexus",
  "private": true,
  "scripts": {
    "fetch:dashboard": "bash fetch-dashboard.sh",
    "install:all": "npm run fetch:dashboard && npm --prefix cli install",
    "start": "npm --prefix cli start",
    "setup": "npm --prefix cli run setup",
    "build": "npm --prefix cli run pkg:build"
  },
  "workspaces": [
    "cli"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
